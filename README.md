# This is a neural network to create mass styles for xaml wpf programs
Requirements:
- Install tensorflow for your CPU or GPU
- Install requirements.txt
- Run train.py for training or run.py for running trained model