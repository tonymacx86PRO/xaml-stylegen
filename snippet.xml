<Style x:Key="CustomButtonStyle" TargetType="Button">
    <Setter Property="Background" Value="Turquoise" />
    <Setter Property="Foreground" Value="Black" />
    <Setter Property="FontSize" Value="20" />
    <Setter Property="FocusVisualStyle" Value="{x:Null}" /> <!-- (не обязательно) -->
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="HorizontalContentAlignment" Value="Center" />
    <Setter Property="Template">
        <Setter.Value>
            <ControlTemplate TargetType="{x:Type Button}">
                <Border Background="{TemplateBinding Background}" Cursor="Hand">
                    <Border x:Name="border" BorderBrush="Yellow" BorderThickness="1" Background="Transparent">
                        <ContentPresenter VerticalAlignment="{TemplateBinding VerticalContentAlignment}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" ContentSource="{TemplateBinding Content}" />
                    </Border>
                </Border>
                <ControlTemplate.Triggers>
					<!-- Если кнопка в фокусе (не обязательно) -->
                    <Trigger Property="Button.IsFocused" Value="True">
                        <Setter TargetName="border" Property="BorderThickness" Value="2" />
                        <Setter TargetName="border" Property="BorderBrush" Value="Black" />
                    </Trigger>
					
					<!-- Если кнопка выключена (не обязательно) -->
                    <Trigger Property="Button.IsEnabled" Value="False">
                        <Setter Property="Background" Value="Gray" />
                        <Setter TargetName="border" Property="BorderBrush" Value="Gray" />
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>
        </Setter.Value>
    </Setter>

	<!-- скругление (не обязательно) -->
    <Style.Resources>
        <Style TargetType="Border">
            <Setter Property="CornerRadius" Value="10" />
        </Style>
    </Style.Resources>

    <Style.Triggers>
		<!-- мышка навелась на кнопку (не обязательно) -->
        <EventTrigger RoutedEvent="Button.MouseEnter">
            <EventTrigger.Actions>
                <BeginStoryboard>
                    <Storyboard>
                        <ColorAnimation
              Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"
              To="Yellow" Duration="0:0:0.5" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger.Actions>
        </EventTrigger>

		<!-- мышка перестала наводиться на кнопку (не обязательно) -->
        <EventTrigger RoutedEvent="Button.MouseLeave">
            <EventTrigger.Actions>
                <BeginStoryboard>
                    <Storyboard>
                        <ColorAnimation
              Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"
              To="Turquoise" Duration="0:0:0.5" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger.Actions>
        </EventTrigger>
    </Style.Triggers>
</Style>